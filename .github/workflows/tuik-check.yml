name: TUİK Kontrol

on:
  workflow_dispatch:

  schedule:
    - cron: '0 6 * * *'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: echo "Merhaba Turgut, workflow başarıyla çalıştı."
